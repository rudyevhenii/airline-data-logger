services:
  db:
    image: mysql:8.0
    container_name: airline_db
    ports:
      - '3306:3306'
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: airline_data_logger
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data: